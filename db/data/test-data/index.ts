import { users } from "./users-data";
import { apps } from "./apps-data";
import { ratings } from "./ratings-data";
import { comments } from "./comments-data";
import { issues } from "./issues-data";
import { categories } from "./categories-data";

export const userData = users;
export const appsData = apps;
export const ratingsData = ratings;
export const commentsData = comments;
export const issuesData = issues;
export const categoriesData = categories;
